name: Compilar macOS Intel nao assinado

on:
  workflow_dispatch:
  push:
    paths:
      - ".github/workflows/build-macos-intel-unsigned.yml"
      - "scripts/build-macos.sh"
      - "scripts/build-sunbrowser150-macos-x64.sh"
      - "scripts/test-macos-unsigned-installer.sh"

permissions:
  contents: write

jobs:
  build:
    name: Painel e instalador completo Intel
    runs-on: macos-15-intel
    timeout-minutes: 60

    env:
      MAC_ALLOW_UNSIGNED: "1"
      ADSPOWER_MAC_X64_URL: "https://version.adspower.net/software/darwin-global/8.6.3/AdsPower-Global-8.6.3.dmg"
      ADSPOWER_MAC_X64_SHA256: "6f30427000ba4f9a1713823008039bece87717bb5d2f6267722a374521a722fe"

    steps:
      - name: Baixar codigo
        uses: actions/checkout@v6

      - name: Confirmar arquitetura e versao
        run: |
          test "$(uname -m)" = "x86_64"
          node -p "require('./package.json').version" | grep -qx "2.0.6"

      - name: Preparar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm

      - name: Instalar dependencias
        run: npm ci

      - name: Compilar macOS Intel
        run: npm run build:mac:intel

      - name: Remover copia temporaria antes do teste de instalacao
        run: rm -rf dist-macos

      - name: Testar instalacao, assinatura interna e Gatekeeper
        env:
          EXPECTED_ARCH: x86_64
          EXPECTED_VERSION: 2.0.6
          REPORT_SLUG: MAC-INTEL
          REPORT_TITLE: macOS Intel
        run: |
          chmod +x scripts/test-macos-unsigned-installer.sh
          scripts/test-macos-unsigned-installer.sh \
            ARQUIVOS-GERADOS/2-INSTALACAO-COMPLETA-NOVOS-CLIENTES/MAC/NinjaFlixCompletoSetup-2.0.6-mac-x64.pkg \
            ARQUIVOS-GERADOS/RELATORIOS/MAC-INTEL-ADHOC
          cat ARQUIVOS-GERADOS/RELATORIOS/MAC-INTEL-ADHOC/RELATORIO-TESTE-MAC-INTEL-2.0.6-ADHOC.md >> "$GITHUB_STEP_SUMMARY"

      - name: Mostrar arquivos gerados
        run: |
          find ARQUIVOS-GERADOS -maxdepth 6 -type f -print
          du -sh ARQUIVOS-GERADOS

      - name: Disponibilizar arquivos para download temporario
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          TAG="build-2.0.6-temp"
          gh release view "$TAG" >/dev/null 2>&1 || \
            gh release create "$TAG" --target "$GITHUB_SHA" --title "Build temporario 2.0.6" --notes "Arquivos de compilacao para publicacao na VPS." --prerelease || \
            gh release view "$TAG" >/dev/null
          gh release upload "$TAG" \
            ARQUIVOS-GERADOS/1-ATUALIZACAO-PAINEL-PUBLICAR-NO-ADMIN/MAC/*-x64.zip \
            ARQUIVOS-GERADOS/2-INSTALACAO-COMPLETA-NOVOS-CLIENTES/MAC/*-x64.pkg \
            ARQUIVOS-GERADOS/2-INSTALACAO-COMPLETA-NOVOS-CLIENTES/MAC/*-x64.txt \
            ARQUIVOS-GERADOS/RELATORIOS/MAC-INTEL-ADHOC/*.md \
            --clobber

      - name: Anexar diagnosticos do teste
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: diagnostico-mac-intel-adhoc-2.0.6
          path: ARQUIVOS-GERADOS/RELATORIOS/MAC-INTEL-ADHOC
          retention-days: 30

  sunbrowser:
    name: SunBrowser 150 Intel
    runs-on: macos-15-intel
    timeout-minutes: 45

    steps:
      - name: Baixar codigo
        uses: actions/checkout@v6

      - name: Confirmar arquitetura
        run: test "$(uname -m)" = "x86_64"

      - name: Reempacotar e testar SunBrowser 150 Intel
        env:
          SUNBROWSER150_UNIVERSAL_PKG_URL: "https://painel.ninjaflix.club/downloads/sunbrowser-private/307abea4-cb57-4a28-9ba1-ae3080935c97"
          SUNBROWSER150_UNIVERSAL_SHA256: "15ebbd37e8e2919b19751df442bbdc8fa2120aad5e063383e4d27038853adae5"
        run: |
          chmod +x scripts/repack-sunbrowser150-macos-x64.sh
          scripts/repack-sunbrowser150-macos-x64.sh

      - name: Publicar SunBrowser no build temporario
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          TAG="build-2.0.6-temp"
          gh release upload "$TAG" \
            ARQUIVOS-GERADOS/3-INSTALADORES-AUXILIARES/SUNBROWSER-150-MAC-X64/* \
            --clobber

      - name: Anexar SunBrowser Intel
        uses: actions/upload-artifact@v4
        with:
          name: sunbrowser-150-macos-x64
          path: ARQUIVOS-GERADOS/3-INSTALADORES-AUXILIARES/SUNBROWSER-150-MAC-X64
          retention-days: 7
